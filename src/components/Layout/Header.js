import React, { Fragment } from 'react';
import classes from './Header.module.css';
const Header = (props) => {
  return;
  <Fragment>
    <header className={classes.Header}>Header will come</header>
    <div>Image will come here</div>
  </Fragment>;
};
export default Header;
